syntax = "proto3";

package aorange;
option go_package="./aorange";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Aorange {
  rpc Ping(Request) returns(Response);
}
